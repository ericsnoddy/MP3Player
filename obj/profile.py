import pygame

class Profile:
    def __init__(self, music_folder, collection, name):

            self.music_folder = music_folder
            self.collection = collection
            self.name = name
